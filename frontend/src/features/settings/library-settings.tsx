import { useEffect, useState, type FormEvent } from "react"
import { LoaderCircle } from "lucide-react"

import { getLibraryPreferences, updateLibraryPreferences, type LibraryPreferences } from "@/api/client"
import { Button } from "@/components/ui/button"
import { sortOptions } from "@/features/media/format"

export function LibrarySettings(){const[value,setValue]=useState<LibraryPreferences|null>(null);const[error,setError]=useState<string|null>(null);const[saving,setSaving]=useState(false);useEffect(()=>{void getLibraryPreferences().then(setValue).catch((cause)=>setError(cause instanceof Error?cause.message:"Preferences could not be loaded."))},[]);async function save(event:FormEvent){event.preventDefault();if(!value)return;setSaving(true);setError(null);try{setValue(await updateLibraryPreferences(value))}catch(cause){setError(cause instanceof Error?cause.message:"Preferences could not be saved.")}finally{setSaving(false)}};if(!value&&!error)return <div className="h-40 animate-pulse rounded-lg bg-muted"/>;return <section className="rounded-lg border bg-card shadow-xs"><div className="border-b px-5 py-4"><h2 className="font-semibold">Library defaults</h2><p className="mt-1 text-sm text-muted-foreground">Applied when a Library view has no explicit URL sort or view selection.</p></div>{value&&<form className="space-y-5 p-5" onSubmit={(event)=>void save(event)}><div><label htmlFor="default-library-sort" className="text-sm font-medium">Default sort</label><select id="default-library-sort" className="mt-2 h-10 w-full rounded-md border bg-background px-3 text-sm" value={value.defaultLibrarySort} onChange={(event)=>setValue({...value,defaultLibrarySort:event.target.value})}>{sortOptions.map(([key,label])=><option key={key} value={key}>{label}</option>)}</select></div><div><label htmlFor="preferred-library-view" className="text-sm font-medium">Preferred view</label><select id="preferred-library-view" className="mt-2 h-10 w-full rounded-md border bg-background px-3 text-sm" value={value.preferredView} onChange={(event)=>setValue({...value,preferredView:event.target.value as "grid"|"list"})}><option value="grid">Artwork grid</option><option value="list">Compact list</option></select></div>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}<Button type="submit" disabled={saving}>{saving&&<LoaderCircle className="animate-spin"/>}{saving?"Saving…":"Save defaults"}</Button></form>}{!value&&error&&<p className="p-5 text-sm text-destructive">{error}</p>}</section>}
