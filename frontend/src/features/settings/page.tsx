import { Settings } from "lucide-react"

import { PlaceholderPage } from "@/components/shared/placeholder-page"

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Settings and integration configuration belong to later phases and are not available yet."
      icon={Settings}
    />
  )
}
