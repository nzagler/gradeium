import { useEffect, useState, type FormEvent } from "react"
import { CircleAlert, CircleCheck, LoaderCircle, PlugZap } from "lucide-react"

import { configureIntegration, getIntegrations, testIntegration, type IntegrationConfiguration, type IntegrationView } from "@/api/client"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"

const providerCopy = {
  igdb: { name: "IGDB", purpose: "Game search, metadata, artwork, and community user ratings.", secretLabel: "Twitch client secret" },
  tmdb: { name: "TMDB", purpose: "Movie metadata and verified TV community ratings.", secretLabel: "API Read Access Token" },
  tvdb: { name: "TVDB", purpose: "Authoritative TV series, season, episode, cast, and artwork metadata.", secretLabel: "TVDB API key" },
} as const

export function IntegrationsPanel() {
  const [values,setValues]=useState<IntegrationView[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null)
  async function load(){setLoading(true);setError(null);try{setValues((await getIntegrations()).integrations)}catch(cause){setError(cause instanceof Error?cause.message:"Integrations could not be loaded.")}finally{setLoading(false)}}
  useEffect(()=>{
    let cancelled=false
    getIntegrations().then((response)=>{if(!cancelled)setValues(response.integrations)}).catch((cause:unknown)=>{if(!cancelled)setError(cause instanceof Error?cause.message:"Integrations could not be loaded.")}).finally(()=>{if(!cancelled)setLoading(false)})
    return()=>{cancelled=true}
  },[])
  if(loading)return <div className="h-48 animate-pulse rounded-lg bg-muted"/>;if(error)return <div className="rounded-lg border bg-card p-6"><p className="text-sm">{error}</p><Button className="mt-3" variant="outline" onClick={()=>void load()}>Try again</Button></div>
  return <div className="space-y-4">{values.map((value)=><ProviderCard key={value.provider} value={value} changed={(next)=>setValues((current)=>current.map((item)=>item.provider===next.provider?next:item))}/>)}</div>
}

function ProviderCard({value,changed}:{value:IntegrationView;changed:(value:IntegrationView)=>void}){
  const copy=providerCopy[value.provider];const[input,setInput]=useState<IntegrationConfiguration>({enabled:value.enabled,clientId:value.clientId??"",secret:"",removeSecret:false,pin:"",removePin:false});const[saving,setSaving]=useState(false);const[testing,setTesting]=useState(false);const[message,setMessage]=useState<string|null>(null);const[failed,setFailed]=useState(false)
  async function save(event:FormEvent){event.preventDefault();setSaving(true);setMessage(null);setFailed(false);try{const next=await configureIntegration(value.provider,input);changed(next);setInput({...input,secret:"",pin:"",removeSecret:false,removePin:false});setMessage("Configuration saved. Test the connection before using this provider.")}catch(cause){setMessage(cause instanceof Error?cause.message:"Configuration could not be saved.");setFailed(true)}finally{setSaving(false)}}
  async function test(){setTesting(true);setMessage(null);setFailed(false);try{const result=await testIntegration(value.provider);setMessage(result.message);changed({...value,state:"connected",lastTest:{provider:value.provider,...result}})}catch(cause){setMessage(cause instanceof Error?cause.message:"Connection test failed.");setFailed(true);changed({...value,state:"error"})}finally{setTesting(false)}}
  const stateLabel=value.state.replaceAll("_"," ")
  return <section className="rounded-lg border bg-card shadow-xs"><div className="flex flex-wrap items-start justify-between gap-4 border-b px-5 py-4"><div><div className="flex items-center gap-2"><PlugZap className="size-4"/><h2 className="font-semibold">{copy.name}</h2></div><p className="mt-1 text-sm text-muted-foreground">{copy.purpose}</p></div><span className={`rounded-full border px-2.5 py-1 text-xs font-medium capitalize ${value.state==="connected"?"border-emerald-300 text-emerald-700":value.state==="error"?"border-red-300 text-destructive":"text-muted-foreground"}`}>{stateLabel}</span></div><form className="space-y-4 p-5" onSubmit={(event)=>void save(event)}><label className="flex items-center gap-3 text-sm"><input type="checkbox" checked={input.enabled} onChange={(event)=>setInput({...input,enabled:event.target.checked})} className="size-4"/>Enable {copy.name}</label>{value.provider==="igdb"&&<div><Label htmlFor="igdb-client-id">Twitch client ID</Label><Input id="igdb-client-id" className="mt-2" value={input.clientId} onChange={(event)=>setInput({...input,clientId:event.target.value})} autoComplete="off"/></div>}<div><Label htmlFor={`${value.provider}-secret`}>{copy.secretLabel}</Label><Input id={`${value.provider}-secret`} className="mt-2" type="password" value={input.secret} onChange={(event)=>setInput({...input,secret:event.target.value,removeSecret:false})} autoComplete="new-password" placeholder={value.secretConfigured?"Saved — enter a replacement":"Not configured"}/><p className="mt-1 text-xs text-muted-foreground">Saved values are encrypted and never displayed again.</p>{value.secretConfigured&&<label className="mt-2 flex items-center gap-2 text-xs text-muted-foreground"><input type="checkbox" checked={input.removeSecret} onChange={(event)=>setInput({...input,removeSecret:event.target.checked,secret:event.target.checked?"":input.secret})}/>Remove saved credential when saving</label>}</div>{value.provider==="tvdb"&&<div><Label htmlFor="tvdb-pin">Subscriber PIN <span className="font-normal text-muted-foreground">(optional)</span></Label><Input id="tvdb-pin" className="mt-2" type="password" value={input.pin} onChange={(event)=>setInput({...input,pin:event.target.value,removePin:false})} autoComplete="new-password" placeholder={value.pinConfigured?"Saved — enter a replacement":"Only for a user-supported key"}/>{value.pinConfigured&&<label className="mt-2 flex items-center gap-2 text-xs text-muted-foreground"><input type="checkbox" checked={input.removePin} onChange={(event)=>setInput({...input,removePin:event.target.checked,pin:event.target.checked?"":input.pin})}/>Remove saved PIN when saving</label>}</div>}<div className="flex flex-wrap items-center gap-2"><Button type="submit" disabled={saving}>{saving&&<LoaderCircle className="animate-spin"/>}{saving?"Saving…":"Save configuration"}</Button><Button type="button" variant="outline" disabled={testing||!value.configured} onClick={()=>void test()}>{testing&&<LoaderCircle className="animate-spin"/>}{testing?"Testing…":"Test connection"}</Button>{message&&<span role={failed?"alert":"status"} className={`inline-flex items-center gap-1 text-sm ${failed?"text-destructive":"text-muted-foreground"}`}>{failed?<CircleAlert className="size-4"/>:<CircleCheck className="size-4"/>}{message}</span>}</div>{value.lastTest&&<p className="text-xs text-muted-foreground">Last tested {new Date(value.lastTest.testedAt).toLocaleString()} · {value.lastTest.message}</p>}</form></section>
}
